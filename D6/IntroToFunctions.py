# recognise the built-in functions
# we always have the ability to define / create our own functions
# similar to other languages, but don't need to specify the return data type / static / non - static

def my_func():
    print("Hello\nBye") # take note of the indentation, lines within the function must be intended

my_func()


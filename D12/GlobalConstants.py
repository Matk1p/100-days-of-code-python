# sometimes it's very useful to use global scopes
# we can do this with global constants -> if we know we pretty much never gonna change it

PI =3.142159
URL = "google.com"

# so this way, we can refer to these global variables.
# The naming convention for these constant variables are that they're all typed in caps lock

